<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="leonardoberti.example.sapnet" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sapnet" >
        <activity
            android:name=".ChooseCourse"
            android:exported="false"
            android:theme="@style/Theme.CercaCorso"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".SignUp_AggiungiCorsoPreferito"
            android:exported="false"
            android:theme="@style/Theme.SchermateBenvenuto" />
        <activity
            android:name=".CreatePost"
            android:exported="false" />
        <activity
            android:name=".SignUp"
            android:exported="false" />
        <activity
            android:name=".HomePage"
            android:exported="false"
            android:theme="@style/Theme.SapnetHomePage" />
        <activity
            android:name=".Login"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>